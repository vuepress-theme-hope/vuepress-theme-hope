name: Deploy V2 Docs and demos

on:
  push:
    tags:
      - v2.**

jobs:
  deploy-netlify-docs:
    name: Deploy V2 docs to Netlify
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3
        with:
          fetch-depth: 0

      - uses: actions/setup-node@v3
        with:
          node-version: "16"

      - name: restore lerna
        uses: actions/cache@v3
        id: lerna-cache
        with:
          path: |
            node_modules
            **/node_modules
          key: ${{ runner.os }}-lerna-${{ hashFiles('yarn.lock') }}

      - name: Get yarn cache directory path
        if: steps.lerna-cache.outputs.cache-hit != 'true'
        id: yarn-cache-dir-path
        run: echo "::set-output name=dir::$(yarn cache dir)"

      - name: yarn-cache
        if: steps.lerna-cache.outputs.cache-hit != 'true'
        uses: actions/cache@v3
        id: yarn-cache
        with:
          path: ${{ steps.yarn-cache-dir-path.outputs.dir }}
          key: ${{ runner.os }}-yarn-${{ hashFiles('yarn.lock') }}
          restore-keys: |
            ${{ runner.os }}-yarn-

      - name: Install Deps
        run: yarn install --frozen-lockfile

      - name: Build project
        env:
          NODE_OPTIONS: --max_old_space_size=4096
        run: yarn run build

      - name: Docs build
        run: yarn run docs:vite-build

      - name: Combine output
        run: |
          yarn cpx "docs/add-this/dist/**" dist/add-this
          yarn cpx "docs/blog2/dist/**" dist/blog
          yarn cpx "docs/comment2/dist/**" dist/comment
          yarn cpx "docs/components/dist/**" dist/components
          yarn cpx "docs/copy-code2/dist/**" dist/copy-code
          yarn cpx "docs/feed2/dist/**" dist/feed
          yarn cpx "docs/lightgallery/dist/**" dist/lightgallery
          yarn cpx "docs/md-enhance/dist/**" dist/md-enhance
          yarn cpx "docs/photo-swipe/dist/**" dist/photo-swipe
          yarn cpx "docs/pwa2/dist/**" dist/pwa
          yarn cpx "docs/reading-time2/dist/**" dist/reading-time
          yarn cpx "docs/sass-palette/dist/**" dist/sass-palette
          yarn cpx "docs/seo2/dist/**" dist/seo
          yarn cpx "docs/sitemap2/dist/**" dist/sitemap
          yarn cpx "docs/theme/dist/**" dist

      - name: Deploy
        uses: JamesIves/github-pages-deploy-action@v4
        with:
          repository-name: vuepress-theme-hope/v2
          branch: netlify
          folder: dist
          token: ${{ secrets.ACCESS_TOKEN }}

  deploy-gitee-docs:
    name: Deploy V2 docs to Gitee
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3
        with:
          fetch-depth: 0

      - uses: actions/setup-node@v3
        with:
          node-version: "16"

      - name: restore lerna
        uses: actions/cache@v3
        id: lerna-cache
        with:
          path: |
            node_modules
            **/node_modules
          key: ${{ runner.os }}-lerna-${{ hashFiles('yarn.lock') }}

      - name: Get yarn cache directory path
        if: steps.lerna-cache.outputs.cache-hit != 'true'
        id: yarn-cache-dir-path
        run: echo "::set-output name=dir::$(yarn cache dir)"

      - name: yarn-cache
        if: steps.lerna-cache.outputs.cache-hit != 'true'
        uses: actions/cache@v3
        id: yarn-cache
        with:
          path: ${{ steps.yarn-cache-dir-path.outputs.dir }}
          key: ${{ runner.os }}-yarn-${{ hashFiles('yarn.lock') }}
          restore-keys: |
            ${{ runner.os }}-yarn-

      - name: Install Deps
        run: yarn install --frozen-lockfile

      - name: Build project
        env:
          NODE_OPTIONS: --max_old_space_size=4096
        run: yarn run build

      - name: Docs build
        env:
          BASE: /v2/
          HOSTNAME: https://vuepress-theme-hope.gitee.io/
        run: yarn run docs:vite-build

      - name: Combine output
        run: |
          yarn cpx "docs/add-this/dist/**" dist/add-this
          yarn cpx "docs/blog2/dist/**" dist/blog
          yarn cpx "docs/comment2/dist/**" dist/comment
          yarn cpx "docs/components/dist/**" dist/components
          yarn cpx "docs/copy-code2/dist/**" dist/copy-code
          yarn cpx "docs/feed2/dist/**" dist/feed
          yarn cpx "docs/lightgallery/dist/**" dist/lightgallery
          yarn cpx "docs/md-enhance/dist/**" dist/md-enhance
          yarn cpx "docs/photo-swipe/dist/**" dist/photo-swipe
          yarn cpx "docs/pwa2/dist/**" dist/pwa
          yarn cpx "docs/reading-time2/dist/**" dist/reading-time
          yarn cpx "docs/sass-palette/dist/**" dist/sass-palette
          yarn cpx "docs/seo2/dist/**" dist/seo
          yarn cpx "docs/sitemap2/dist/**" dist/sitemap
          yarn cpx "docs/theme/dist/**" dist

      - name: Deploy
        uses: JamesIves/github-pages-deploy-action@v4
        with:
          repository-name: vuepress-theme-hope/v2
          branch: gitee
          folder: dist
          token: ${{ secrets.ACCESS_TOKEN }}

  deploy-theme-demo:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - uses: actions/setup-node@v3
        with:
          node-version: "16"

      - name: restore lerna
        uses: actions/cache@v3
        id: lerna-cache
        with:
          path: |
            node_modules
            **/node_modules
          key: ${{ runner.os }}-lerna-${{ hashFiles('yarn.lock') }}

      - name: Get yarn cache directory path
        if: steps.lerna-cache.outputs.cache-hit != 'true'
        id: yarn-cache-dir-path
        run: echo "::set-output name=dir::$(yarn cache dir)"

      - name: yarn-cache
        if: steps.lerna-cache.outputs.cache-hit != 'true'
        uses: actions/cache@v3
        id: yarn-cache
        with:
          path: ${{ steps.yarn-cache-dir-path.outputs.dir }}
          key: ${{ runner.os }}-yarn-${{ hashFiles('yarn.lock') }}
          restore-keys: |
            ${{ runner.os }}-yarn-

      - name: Install Deps
        run: yarn install --frozen-lockfile

      - name: Build project
        env:
          NODE_OPTIONS: --max_old_space_size=4096
        run: yarn run build

      - name: Build Netlify Demo
        run: yarn workspace theme-demo demo:vite-build

      - name: Deploy to netlify
        uses: JamesIves/github-pages-deploy-action@v4
        with:
          repository-name: vuepress-theme-hope/v2-demo
          branch: netlify
          folder: demo/theme/dist
          token: ${{ secrets.ACCESS_TOKEN }}

      - name: Build Gitee Demo
        env:
          BASE: /v2-demo/
          HOSTNAME: https://vuepress-theme-hope.gitee.io/
        run: yarn workspace theme-demo demo:vite-build

      - name: Deploy to gitee
        uses: JamesIves/github-pages-deploy-action@v4
        with:
          repository-name: vuepress-theme-hope/v2-demo
          branch: gitee
          folder: demo/theme/dist
          token: ${{ secrets.ACCESS_TOKEN }}

  deploy-demo:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - uses: actions/setup-node@v3
        with:
          node-version: "16"

      - name: restore lerna
        uses: actions/cache@v3
        id: lerna-cache
        with:
          path: |
            node_modules
            **/node_modules
          key: ${{ runner.os }}-lerna-${{ hashFiles('yarn.lock') }}

      - name: Get yarn cache directory path
        if: steps.lerna-cache.outputs.cache-hit != 'true'
        id: yarn-cache-dir-path
        run: echo "::set-output name=dir::$(yarn cache dir)"

      - name: yarn-cache
        if: steps.lerna-cache.outputs.cache-hit != 'true'
        uses: actions/cache@v3
        id: yarn-cache
        with:
          path: ${{ steps.yarn-cache-dir-path.outputs.dir }}
          key: ${{ runner.os }}-yarn-${{ hashFiles('yarn.lock') }}
          restore-keys: |
            ${{ runner.os }}-yarn-

      - name: Install Deps
        run: yarn install --frozen-lockfile

      - name: Build project
        env:
          NODE_OPTIONS: --max_old_space_size=4096
        run: yarn run build

      - name: Build add-this demo
        env:
          BASE: /add-this-demo/
          HOSTNAME: https://vuepress-theme-hope.github.io/
        run: yarn workspace add-this-demo demo:vite-build

      - name: Deploy add-this demo
        uses: JamesIves/github-pages-deploy-action@v4
        with:
          repository-name: vuepress-theme-hope/add-this-demo
          branch: gh-pages
          folder: demo/add-this/src/.vuepress/dist
          token: ${{ secrets.ACCESS_TOKEN }}

      - name: Build blog2 demo
        env:
          BASE: /blog2-demo/
          HOSTNAME: https://vuepress-theme-hope.github.io/
        run: yarn workspace blog2-demo demo:vite-build

      - name: Deploy blog2 demo
        uses: JamesIves/github-pages-deploy-action@v4
        with:
          repository-name: vuepress-theme-hope/blog2-demo
          branch: gh-pages
          folder: demo/blog2/src/.vuepress/dist
          token: ${{ secrets.ACCESS_TOKEN }}

      - name: Build comment2 demo
        env:
          BASE: /comment2-demo/
          HOSTNAME: https://vuepress-theme-hope.github.io/
        run: yarn workspace comment2-demo demo:vite-build

      - name: Deploy comment2 demo
        uses: JamesIves/github-pages-deploy-action@v4
        with:
          repository-name: vuepress-theme-hope/comment2-demo
          branch: gh-pages
          folder: demo/comment2/src/.vuepress/dist
          token: ${{ secrets.ACCESS_TOKEN }}

      - name: Build copy-code2 demo
        env:
          BASE: /copy-code2-demo/
          HOSTNAME: https://vuepress-theme-hope.github.io/
        run: yarn workspace copy-code2-demo demo:vite-build

      - name: Deploy copy-code2 demo
        uses: JamesIves/github-pages-deploy-action@v4
        with:
          repository-name: vuepress-theme-hope/copy-code2-demo
          branch: gh-pages
          folder: demo/copy-code2/src/.vuepress/dist
          token: ${{ secrets.ACCESS_TOKEN }}

      - name: Build feed2 demo
        env:
          BASE: /feed2-demo/
          HOSTNAME: https://vuepress-theme-hope.github.io/
        run: yarn workspace feed2-demo demo:vite-build

      - name: Deploy feed2 demo
        uses: JamesIves/github-pages-deploy-action@v4
        with:
          repository-name: vuepress-theme-hope/feed2-demo
          branch: gh-pages
          folder: demo/feed2/src/.vuepress/dist
          token: ${{ secrets.ACCESS_TOKEN }}

      - name: Build lightgallery demo
        env:
          BASE: /lightgallery-demo/
          HOSTNAME: https://vuepress-theme-hope.github.io/
        run: yarn workspace lightgallery-demo demo:vite-build

      - name: Deploy lightgallery demo
        uses: JamesIves/github-pages-deploy-action@v4
        with:
          repository-name: vuepress-theme-hope/lightgallery-demo
          branch: gh-pages
          folder: demo/lightgallery/src/.vuepress/dist
          token: ${{ secrets.ACCESS_TOKEN }}

      - name: Build md-enhance demo
        env:
          BASE: /md-enhance-demo/
          HOSTNAME: https://vuepress-theme-hope.github.io/
        run: yarn workspace md-enhance-demo demo:vite-build

      - name: Deploy md-enhance demo
        uses: JamesIves/github-pages-deploy-action@v4
        with:
          repository-name: vuepress-theme-hope/md-enhance-demo
          branch: gh-pages
          folder: demo/md-enhance/src/.vuepress/dist
          token: ${{ secrets.ACCESS_TOKEN }}

      - name: Build photo-swipe demo
        env:
          BASE: /photo-swipe-demo/
          HOSTNAME: https://vuepress-theme-hope.github.io/
        run: yarn workspace photo-swipe-demo demo:vite-build

      - name: Deploy photo-swipe demo
        uses: JamesIves/github-pages-deploy-action@v4
        with:
          repository-name: vuepress-theme-hope/photo-swipe-demo
          branch: gh-pages
          folder: demo/photo-swipe/src/.vuepress/dist
          token: ${{ secrets.ACCESS_TOKEN }}

      - name: Build pwa2 demo
        env:
          BASE: /pwa2-demo/
          HOSTNAME: https://vuepress-theme-hope.github.io/
        run: yarn workspace pwa2-demo demo:vite-build

      - name: Deploy pwa2 demo
        uses: JamesIves/github-pages-deploy-action@v4
        with:
          repository-name: vuepress-theme-hope/pwa2-demo
          branch: gh-pages
          folder: demo/pwa2/src/.vuepress/dist
          token: ${{ secrets.ACCESS_TOKEN }}

      - name: Build sass-palette demo
        env:
          BASE: /sass-palette-demo/
          HOSTNAME: https://vuepress-theme-hope.github.io/
        run: yarn workspace sass-palette-demo demo:vite-build

      - name: Deploy sass-palette demo
        uses: JamesIves/github-pages-deploy-action@v4
        with:
          repository-name: vuepress-theme-hope/sass-palette-demo
          branch: gh-pages
          folder: demo/sass-palette/src/.vuepress/dist
          token: ${{ secrets.ACCESS_TOKEN }}

      - name: Build seo2 demo
        env:
          BASE: /seo2-demo/
          HOSTNAME: https://vuepress-theme-hope.github.io/
        run: yarn workspace seo2-demo demo:vite-build

      - name: Deploy seo2 demo
        uses: JamesIves/github-pages-deploy-action@v4
        with:
          repository-name: vuepress-theme-hope/seo2-demo
          branch: gh-pages
          folder: demo/seo2/src/.vuepress/dist
          token: ${{ secrets.ACCESS_TOKEN }}

      - name: Build sitemap2 demo
        env:
          BASE: /sitemap2-demo/
          HOSTNAME: https://vuepress-theme-hope.github.io/
        run: yarn workspace sitemap2-demo demo:vite-build

      - name: Deploy sitemap2 demo
        uses: JamesIves/github-pages-deploy-action@v4
        with:
          repository-name: vuepress-theme-hope/sitemap2-demo
          branch: gh-pages
          folder: demo/sitemap2/src/.vuepress/dist
          token: ${{ secrets.ACCESS_TOKEN }}
