export interface HopeThemeBehaviorOptions {
  /**
   * Whether perform extra checks
   *
   * @default false
   */
  check?: boolean;

  /**
   * Whether compact with beta versions
   *
   * @default false
   */
  compact?: boolean;

  /**
   * Whether enable debug mode
   *
   * @default false
   */
  debug?: boolean;
}
