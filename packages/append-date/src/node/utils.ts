import { Logger } from "vuepress-shared/node";

export const PLUGIN_NAME = "vuepress-plugin-append-date";

export const GIT_PLUGIN_NAME = "@vuepress/plugin-git";

export const logger = new Logger(PLUGIN_NAME);
