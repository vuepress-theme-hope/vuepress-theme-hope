.theme-dark
  // 分页插件
  .pagation-wrapper
    .pagation-list
      .btn-group
        bordercolor()

        div:not(.btn):hover
          box-shadow 0 1px 6px 0 rgba(255, 255, 255, 0.2)

      input
        bgcolor()
        fontcolor()
        bordercolor()

      .navigateBtn
        bordercolor()
