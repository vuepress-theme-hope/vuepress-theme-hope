.theme-dark
  // 侧边栏
  .sidebar
    bgColor()
    borderColor()

    .nav-links a
      fontColor()

  // 导航栏下拉菜单
  .dropdown-wrapper .dropdown-title .title
    &:hover, &.router-link-active
      color $accentColor

  .sidebar-heading
    fontColor()

  // 侧边栏链接
  a.sidebar-link
    fontColor()
