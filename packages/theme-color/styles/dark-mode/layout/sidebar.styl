.theme-dark
  // 侧边栏
  .sidebar
    bgcolor()
    bordercolor()
    box-shadow 2px 0 8px #0f0e0d

    .nav-links a
      fontcolor()

      &:hover, &.router-link-active
        color $accentColor

  .sidebar-heading
    fontcolor()

  // 侧边栏链接
  a.sidebar-link
    fontcolor()
